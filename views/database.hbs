{{!-- Database admin page: Solr index backups (and future backend tools) --}}
{{#> layout title="Database" nav=1 dbActive=1}}

{{#*inline "header-block"}}
    <style>
        .table td.fit,
        .table th.fit {
            white-space: nowrap;
            width: 1%;
        }
    </style>
{{/inline}}

{{#*inline "search-area"}}
<p class="text-body-secondary">Perform administrative actions on the reference database, like creating backups or retabulating reference statistics. Backups must be restored from the command line.</p>

<form class="form inline" id="createBackupForm">
    <div class="mb-3">
        <label for="backupCore">Backup core:</label>
        <select class="form-control" id="backupCore" name="core">
            <option value="rad">References (rad)</option>
            <option value="source">Sources (source)</option>
            <option value="both">Both</option>
        </select>
    </div>
    <button type="submit" class="btn btn-primary" id="createBackupButton">
        <span class="bi bi-floppy2-fill"></span> Create Backup
    </button>
    <span id="backupStatus" class="text-muted" style="margin-left: 1em;"></span>
</form>

<form class="form inline" id="recomputeStatsForm" style="margin-top: 1em;">
    <button type="submit" class="btn btn-info" id="recomputeStatsButton" data-bs-toggle="tooltip"
        title="Scan the rad core and rebuild the latest reference, total record count, and highest id in database.json">
        <span class="bi bi-arrow-repeat"></span> Recompute Stats
    </button>
    <span id="recomputeStatus" class="text-muted" style="margin-left: 1em;" aria-live="polite"></span>
</form>
{{/inline}}

{{#*inline "results-area"}}
<div id="mainDisplay" role="main">
    <div class="table-responsive">
        <table class="table table-sm table-hover">
            <thead>
                <tr>
                    <th>Filename</th>
                    <th class="fit">Core</th>
                    <th class="fit">Created</th>
                    <th class="fit">Size</th>
                    <th class="fit">Delete</th>
                </tr>
            </thead>
            <tbody>
                {{#each backups}}
                <tr>
                    <td>{{name}}</td>
                    <td class="fit">{{core}}</td>
                    <td class="fit">{{mtime}}</td>
                    <td class="fit">{{sizeFormatted}}</td>
                    <td class="fit">
                        <a class="btn btn-secondary delete-backup" data-filename="{{name}}" title="Delete backup">
                            <span class="bi bi-trash3"></span>
                        </a>
                    </td>
                </tr>
                {{/each}}
                {{#unless backups.length}}
                <tr>
                    <td colspan="5" class="text-muted text-center">No backups yet. Use the form above to create one.</td>
                </tr>
                {{/unless}}
            </tbody>
        </table>
    </div>
</div>
{{/inline}}

{{#*inline "modal-block"}}
<div class="modal fade" id="deleteBackupConfirm" role="dialog" tabindex=0>
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Delete this backup?</h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>About to delete <strong id="deleteBackupName"></strong>. This cannot be undone.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-bs-dismiss="modal" id="confirm-delete-backup-button">Delete</button>
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="recomputeResult" role="dialog" tabindex=0>
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Recompute complete</h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p id="recomputeSummary"></p>
                <table class="table table-sm" id="recomputeChangesTable" style="display:none;">
                    <thead>
                        <tr><th>Field</th><th>Before</th><th>After</th></tr>
                    </thead>
                    <tbody id="recomputeChangesBody"></tbody>
                </table>
                <p id="recomputeLatestRef"></p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>
{{/inline}}

{{#*inline "footer-block"}}
    {{#if updated}}<p>Database updated: {{updated}} | Records: {{numRecords}} | Latest: {{latest}}.</p>{{/if}}
  
    {{> footerAdmin}}
{{/inline}}

{{#*inline "scripts-block"}}
<script nonce="{{nonce}}" src="private/databaseTools.js"></script>
{{/inline}}

{{/layout}}
